
import React, { Component } from 'react';

class Icon extends Component {
    render() {
        //const { className, svgIcon } = this.props;
        return (
            /*<svg className={ className } viewBox={ svgIcon.viewBox } role='img'>
                <use role='presentation' xlinkHref={ svgIcon.url } />
            </svg>*/
            <div></div>
        );
    }
}

export default Icon;